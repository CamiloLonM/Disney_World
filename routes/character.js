// COntrolador de ruta via acceso
const { Router } = require("express");

const Character = require("../models/characters");

// llamamos las Rutas get post put etc....
//const {

//}

const router = Router();

module.exports = router;
